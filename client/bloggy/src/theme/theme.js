import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    button: {
      bg: "#87CEEB",
      textcolor: "white",
    },
    bgColor: {
      bg: "#1E272E",
      darkBlue: "#011627",
    },
  },
});

export default theme;
